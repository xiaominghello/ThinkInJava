package a.javalogic.chapter1;

/**
 * 函数返回值类型是void是，return不是必需的，在没有return的情况下，
 * 会执行到函数结尾自动返回。return用于显式结束函数执行，返回调用方。
 *
 * return可以用于函数内的任意地方。
 *
 * 函数返回值类型为void也可以使用return，即 return; 不用带值，
 * 含义是返回调用方，只是没有返回值而已。
 *
 * @author nuc8
 * @date 2020/5/17 12:21 下午
 */
public class ReturnDemo {
}
